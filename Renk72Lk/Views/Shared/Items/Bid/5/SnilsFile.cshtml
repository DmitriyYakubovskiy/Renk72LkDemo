@model CreateBidViewModel
@{
    var isDisabled = Model.Bid.Step5.SnilsFile?.FilePath != null;
}

<div class="lk-block-info lk-block-info--mod">
    <div class="lk-block-info__container">
        <div class="form-group">
            Страховой номер индивидуального лицевого счета в системе индивидуального (перонифицированного) учета (СНИЛС) (Размер файла не более 10 Мб)
        </div>
        <div class="form-group" name="container-@nameof(Model.Bid.Step5.SnilsFiles)">
            <input type="file" class="form-group__file" id="filename_6" multiple disabled="@isDisabled"
                   name="@nameof(Model.Bid.Step5.SnilsFiles)" accept=".jpg, .jpeg, .png, .pdf" asp-for="@Model.Bid.Step5.SnilsFiles">
        </div>

        @if (Model.Bid.Step5.SnilsFile?.FilePath != null)
        {
            <div class="uploaded-file-info" style=" ackground: #f5f5f5; border-radius: 4px;">
                <div style="display: flex; align-items: center; gap: 10px;">
                    <span style="color: green;">✔ Документ загружен:</span>
                    <a href="@Model.Bid.Step5.SnilsFile.FilePath" target="_blank" style="color: #0066cc; text-decoration: underline;">
                        @Model.Bid.Step5.SnilsFile.FileName
                    </a>
                    <button type="button" onclick="removeUploadedFile('snilsFile')" style="margin-left: auto; color: #ff0000; background: none; border: none; cursor: pointer;">
                        Удалить
                    </button>
                </div>
            </div>
        }
    </div>
</div>